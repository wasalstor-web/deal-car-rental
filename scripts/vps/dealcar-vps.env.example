# Copy to dealcar-vps.env on the server (same directory) — loaded by update-dealcar.sh.
# Keeps Mongo + mongo-express off the public interface; web/admin/API ports unchanged.

BOOKCARS_MONGO_BIND=127.0.0.1
BOOKCARS_MONGO_EXPRESS_BIND=127.0.0.1
