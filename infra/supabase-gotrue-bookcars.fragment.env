# انسخ الأسطر التالية إلى نهاية supabase/docker/.env ثم أعد تشغيل حاويات Supabase (docker compose down && up).
# Copy these lines into supabase/docker/.env, then restart Supabase containers.

SITE_URL=http://localhost:13080
ADDITIONAL_REDIRECT_URLS=http://localhost:13080
